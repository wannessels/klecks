version: '3'

services:
    klecks:
        build: .
        container_name: klecks
        ports:
            - 5050:3000
        volumes:
            - chat-data:/var/www/chat-data

volumes:
    chat-data:
